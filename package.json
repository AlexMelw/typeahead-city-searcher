{
  "name": "typeahead-searcher",
  "version": "0.0.1",
  "description": "Typeahead searcher with vanilla JS and module CSS",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel serve index.html",
    "build": "parcel build index.html"
  },
  "keywords": [
    "typeahead",
    "search",
    "webapp"
  ],
  "author": "Slavik",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.9.2"
  }
}
